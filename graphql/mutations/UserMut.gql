mutation createUser($email: String!, $password: String!) {
  signup(email: $email, password: $password) {
    user {
      id
      email
    }
  }
}

mutation Login($email: String!, $password: String!) {
  login(email: $email, password: $password) {
    user {
      id
      email
    }
  }
}

# mutation deleteALoginUser($where: LoginUserWhereUniqueInput!) {
#   deleteOneLoginUser(where: $where) {
#     ...LoginUserFragment
#   }
# }

# mutation updateALoginUser(
#   $data: LoginUserUpdateInput!
#   $where: LoginUserWhereUniqueInput!
# ) {
#   updateOneLoginUser(data: $data, where: $where) {
#     ...LoginUserFragment
#   }
# }
